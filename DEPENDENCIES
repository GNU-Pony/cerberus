RUNTIME DEPENDENCIES:

	libc
	linux (opt-out)
	pam (iff compled with pam support)
	libpassphrase>=1449331105


BUILD DEPENDENCIES:

	pam (opt-out)
	libpassphrase>=1449331105
	cc (but gcc is preferred)
	make
	coreutils
	texinfo (out-put)


INSTALL DEPENDENCIES:

	make
	coreutils

